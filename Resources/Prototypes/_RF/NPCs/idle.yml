- type: htnCompound
  id: RfIdleCompound
  branches:
  - preconditions:
    - !type:InConversationPrecondition
    - !type:ActorsHasTaskPrecondition
      invert: true
    tasks:
    - !type:HTNCompoundTask
      task: ConversationCompound

  # Going to sleep on the bed if we are very tired
  - preconditions:
    - !type:NeedLevelPrecondition
      need: Sleep
      thresholdId: Exhausted
    tasks:
    - !type:HTNPrimitiveTask
      operator: !type:UtilityOperator
        proto: Bed
    - !type:HTNCompoundTask
      task: SleepCompound

  # Or we sleep on the floor if there is no free bed
  - preconditions:
    - !type:NeedLevelPrecondition
      need: Sleep
      thresholdId: Exhausted
    tasks:
    - !type:HTNCompoundTask
      task: SleepOnFloor

  # Looking for food if we are hungry
  - preconditions:
    - !type:HungryPrecondition
      minHungerState: Starving
    tasks:
    - !type:HTNPrimitiveTask
      operator: !type:UtilityOperator
        proto: Food
        key: PickUpTarget
    - !type:HTNCompoundTask
      task: EatCompound

  # Looking for a drink if we're thirsty
  - preconditions:
    - !type:ThirstyPrecondition
      minThirstState: Parched
    tasks:
    - !type:HTNPrimitiveTask
      operator: !type:UtilityOperator
        proto: Drink
        key: PickUpTarget
    - !type:HTNCompoundTask
      task: DrinkCompound

  - tasks:
    - !type:HTNPrimitiveTask
      operator: !type:RoutineTaskOperator

  - tasks:
    - !type:HTNCompoundTask
      task: IdleMoveCompound

- type: htnCompound
  id: RaidIdleCompound
  branches:
  - tasks:
    - !type:HTNPrimitiveTask
      operator: !type:UtilityOperator
        proto: NearbyMeleeTargets
    - !type:HTNCompoundTask
      task: AttackTargetCompound

  - tasks:
    - !type:HTNCompoundTask
      task: IdleMoveCompound

- type: htnCompound
  id: IdleMoveCompound
  branches:
  - preconditions:
    - !type:InContainerPrecondition
      isInContainer: true
    tasks:
    - !type:HTNCompoundTask
      task: EscapeCompound

  # Pick a new spot and wait there.
  - tasks:
    - !type:HTNPrimitiveTask
      operator: !type:PickAccessibleOperator
        rangeKey: IdleRange
    - !type:HTNPrimitiveTask
      operator: !type:MoveToOperator
        pathfindInPlanning: false
    - !type:HTNPrimitiveTask
      operator: !type:RandomOperator
        targetKey: IdleTime
        minKey: MinimumIdleTime
        maxKey: MaximumIdleTime
    - !type:HTNPrimitiveTask
      operator: !type:WaitOperator
        key: IdleTime
